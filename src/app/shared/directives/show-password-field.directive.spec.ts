import { ShowPasswordFieldDirective } from './show-password-field.directive';

describe('ShowPasswordFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowPasswordFieldDirective();
    expect(directive).toBeTruthy();
  });
});
